language: python
python:
  - "3.3"
virtualenv:
  system_site_packages: true
before_install:
  - sudo apt-get install -qq gfortran libatlas-base-dev python-numpy python-matplotlib
install:
  - pip install ipython mock pandas --use-mirrors
script:
  - nosetests